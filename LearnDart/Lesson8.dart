void main(){
  //const in dart
  final x = 100; //can't use var to inference type
  final double pi = 3.145926;
  final String name = "Quoc An";

  const int y = 200; // just create const in compile time
  const friends = const []; //can use const , if u like
  final number = []; //can't use final, can use const
}